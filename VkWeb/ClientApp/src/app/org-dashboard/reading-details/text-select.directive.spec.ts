import { TextSelectEventDirective } from './text-select-event.directive';

describe('TextSelectEventDirective', () => {
  it('should create an instance', () => {
    const directive = new TextSelectEventDirective();
    expect(directive).toBeTruthy();
  });
});
